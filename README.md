# Gmail-Automation-smtp
A modular Python script for secure, batch email automation via Gmail SMTP. Uses python-dotenv for credential safety and email.message for robust MIME handling. Features a persistent SMTP connection to efficiently send multiple emails with attachments in a single session. Perfect for automated reports and notifications.
